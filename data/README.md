# Dataset Directory

The public repository intentionally does not include any dataset images or
dataset metadata. Download the datasets separately and place them under this
directory using the paths expected by the files in `configs/datasets/`.

Few-shot split files and generated prompts are also local-only and are ignored
by Git to prevent accidental publication.
